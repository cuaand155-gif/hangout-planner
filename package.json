{
  "name": "waddle",
  "version": "1.0.0",
  "private": true,
  "description": "Find the time everyone is free, then plan something worth doing.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "test": "node --test \"test/*.test.mjs\""
  }
}
